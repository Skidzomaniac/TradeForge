run:
  timeout: 5m
  go: "1.26"
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - gosimple
issues:
  exclude-rules:
    - path: _test\.go
      linters: [errcheck]
