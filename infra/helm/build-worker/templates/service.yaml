apiVersion: v1
kind: Service
metadata:
  name: {{ include "build-worker.fullname" . }}
  labels: {{- include "build-worker.labels" . | nindent 4 }}
spec:
  selector: { app: build-worker }
  ports: [{ port: {{ .Values.service.port }}, targetPort: {{ .Values.service.port }} }]
