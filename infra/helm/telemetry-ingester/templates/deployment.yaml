apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "telemetry-ingester.fullname" . }}
  labels: {{- include "telemetry-ingester.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: { app: telemetry-ingester }
  template:
    metadata:
      labels: {{- include "telemetry-ingester.labels" . | nindent 8 }}
    spec:
      containers:
        - name: telemetry-ingester
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports: [{ containerPort: {{ .Values.service.port }} }]
          resources: {{- toYaml .Values.resources | nindent 12 }}
          readinessProbe:
            httpGet: { path: /v1/health, port: {{ .Values.service.port }} }
            periodSeconds: 5
