apiVersion: v1
kind: Service
metadata:
  name: orchestrator
  namespace: trade-eval
  labels:
    app: orchestrator
spec:
  type: ClusterIP
  selector:
    app: orchestrator
  ports:
    - name: health
      port: 8082
      targetPort: 8082
